由于目前的整个代码和上面完全不一致了，我这里重新整理下以下的需求，你重新输出所有代码
1. 项目的名称叫 P4VProjectManager
2. 在读取配置阶段，需要检测所有配置的有效性，p4v 是否可以连接（非测试模式下），所有文件目录是否存在
3. 并行执行多个项目的构建，并不是通过多线程的实现，而是启动多个subprocess，也就是需要把启动 process 和 检测 process 这两个阶段给分开，在 检测 process 状态的循环中，会检测所有正在进行的 subprocess 的状态